# NOTE: This file is auto generated by OpenAPI Generator 7.25.0 (https://openapi-generator.tech)
# using the Elixir SDK Generator template (https://github.com/houllette/elixir-sdk-generator).
# Do not edit this file manually — edit the templates and regenerate instead.

defmodule ExDaytona.Model.Chart do
  @moduledoc """
  Chart model.
  """

  # Credential-safe inspection: fields whose names indicate credentials
  # (tokens, keys, secrets, passwords) render as "[REDACTED]" — see
  # ExDaytona.Redact. Regeneration cannot remove this protection
  # because it lives in the model template.
  defimpl Inspect do
    def inspect(struct, opts), do: ExDaytona.Redact.inspect_struct(struct, opts)
  end

  # Encode without nil fields: unset optional fields must be OMITTED from
  # request JSON, not sent as explicit nulls — real servers (Daytona
  # included) reject bodies full of nulls that a bare {} would satisfy.
  defimpl JSON.Encoder do
    def encode(struct, encoder) do
      struct
      |> Map.from_struct()
      |> Enum.reject(fn {_field, value} -> is_nil(value) end)
      |> Map.new()
      |> encoder.(encoder)
    end
  end

  defstruct [
    :elements,
    :png,
    :title,
    :type,
    :x_label,
    :x_scale,
    :x_tick_labels,
    :x_ticks,
    :y_label,
    :y_scale,
    :y_tick_labels,
    :y_ticks
  ]

  @type t :: %__MODULE__{
          :elements => [ExDaytona.Model.ChartElement.t()] | nil,
          :png => String.t() | nil,
          :title => String.t() | nil,
          :type => String.t() | nil,
          :x_label => String.t() | nil,
          :x_scale => String.t() | nil,
          :x_tick_labels => [String.t()] | nil,
          :x_ticks => [number()] | nil,
          :y_label => String.t() | nil,
          :y_scale => String.t() | nil,
          :y_tick_labels => [String.t()] | nil,
          :y_ticks => [number()] | nil
        }

  alias ExDaytona.Deserializer

  def decode(value) do
    value
    |> Deserializer.deserialize(:elements, :list, ExDaytona.Model.ChartElement)
  end
end
